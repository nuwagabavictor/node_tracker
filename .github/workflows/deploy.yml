name: Build and Deploy Restart Finance API

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Build Project
        run: npm run build

      # Remove dev dependencies before uploading
      - name: Keep Production Dependencies Only
        run: |
          npm prune --omit=dev

      - name: Copy Files to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "dist,node_modules,package.json,package-lock.json"
          target: "/var/www/restart.com/backend"

      - name: Restart Application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /var/www/restart.com/backend

            # Restart PM2 (or start if first deployment)
            pm2 reload ecosystem.config.js --update-env || \
            pm2 start ecosystem.config.js

            pm2 save

            pm2 status
